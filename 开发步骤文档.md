下面这版可以直接保存为项目根目录的 `DEVELOPMENT.md`，交给 Codex 按阶段开发。

# ContentFlow 详细开发步骤

## 1. 项目目标

ContentFlow 是一个面向个人内容创作者的 AI 内容策划与生成平台。

用户创建内容项目，填写账号定位、目标受众和内容风格，上传 PDF、Markdown 或 TXT 参考资料，然后在 AI 创作页面输入内容要求。系统通过 LangGraph 工作流解析需求，通过 Chroma 检索当前项目知识库，最终生成结构化文章。

系统需要完整实现以下核心链路：

```text
用户注册登录
→ 创建内容项目
→ 上传项目资料
→ 文档解析与切片
→ 写入 Chroma
→ 输入创作要求
→ LangGraph 解析需求
→ RAG 检索资料
→ 生成结构化内容
→ SSE 流式展示
→ 保存文章
→ 查看和编辑文章
```

第一版优先保证完整闭环可以运行，不开发复杂审核、发布、多 Agent、多租户和数据分析功能。

---

# 2. 技术栈

## 2.1 Java 后端

```text
Java 17
Spring Boot 3.x
Spring Security 6
JWT
MyBatis-Plus
PostgreSQL
Redis
MinIO
WebClient
SSE
Hibernate Validator
Lombok
MapStruct（可选）
Knife4j / SpringDoc
Flyway（推荐）
```

## 2.2 Vue 前端

```text
Vue 3
TypeScript
Vite
Vue Router
Pinia
Element Plus
Axios
Markdown 编辑器
SSE / Fetch Stream
```

## 2.3 Agent 服务

```text
Python 3.12
LangGraph
LangGraph CLI
LangChain
ReAct Agent
Pydantic
Chroma
FastAPI 或 LangGraph Agent Server
HTTPX
PyPDF
文本分块工具
Embedding 模型
```

## 2.4 基础设施

```text
PostgreSQL：业务数据
Redis：缓存和临时状态
MinIO：原始资料文件
Chroma：文档切片和向量
Docker Compose：本地统一部署
Nginx：前端和接口反向代理
```

---

# 3. 项目目录

```text
contentflow/
├── contentflow-backend/
├── contentflow-web/
├── contentflow-agent/
├── sql/
│   ├── init.sql
│   ├── demo-data.sql
│   └── reset.sql
├── deploy/
│   ├── nginx/
│   │   └── nginx.conf
│   └── scripts/
│       ├── start.sh
│       └── stop.sh
├── docs/
│   ├── architecture.md
│   ├── database.md
│   ├── api.md
│   └── development.md
├── .env.example
├── docker-compose.yml
└── README.md
```

---

# 4. 开发原则

Codex 开发时必须遵守以下规则：

1. 后端采用单体 Spring Boot，不拆分微服务。
2. Vue 只能调用 Spring Boot，不直接调用 Agent、Chroma 或 MinIO。
3. Agent 不直接操作 PostgreSQL。
4. Spring Boot 不直接执行向量检索。
5. Agent 需要读取或保存业务数据时，必须调用 Spring Boot 内部接口。
6. 所有项目、文档、内容和会话操作必须校验当前用户归属。
7. 所有 Chroma 检索必须使用 `project_id` 过滤。
8. 所有接口必须使用统一返回结构。
9. 所有接口参数必须进行校验。
10. 所有异常必须经过统一异常处理。
11. 数据库字段名采用蛇形命名。
12. Java 类名采用大驼峰命名。
13. 前端 API 类型必须使用 TypeScript 定义。
14. Agent 输出必须使用 Pydantic 结构化模型。
15. 每完成一个阶段，都要更新 README 中的启动说明和完成功能。
16. 优先保证项目可以启动和联调，不提前开发非核心功能。

---

# 5. 第一阶段：初始化项目

## 5.1 创建根目录

创建以下目录：

```text
contentflow/
├── contentflow-backend/
├── contentflow-web/
├── contentflow-agent/
├── sql/
├── docs/
└── deploy/
```

创建根目录文件：

```text
README.md
.env.example
docker-compose.yml
.gitignore
```

## 5.2 配置 `.gitignore`

至少忽略：

```gitignore
.idea/
.vscode/

*.iml
target/

node_modules/
dist/

.venv/
__pycache__/
*.pyc

.env

data/
logs/

chroma_data/
minio_data/
postgres_data/
redis_data/
```

## 5.3 创建 `.env.example`

```env
POSTGRES_DB=contentflow
POSTGRES_USER=contentflow
POSTGRES_PASSWORD=contentflow123

REDIS_HOST=redis
REDIS_PORT=6379

MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=contentflow

CHROMA_HOST=chroma
CHROMA_PORT=8000

BACKEND_PORT=8080
AGENT_PORT=2024
FRONTEND_PORT=80

JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRE_SECONDS=86400

INTERNAL_AGENT_TOKEN=replace-with-internal-token

LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=

EMBEDDING_API_KEY=
EMBEDDING_BASE_URL=
EMBEDDING_MODEL=
```

## 5.4 第一阶段验收

必须完成：

```text
根目录结构创建完成
三个子工程目录创建完成
.env.example 已提供
.gitignore 已提供
README 包含项目介绍
```

---

# 6. 第二阶段：Docker Compose 基础设施

## 6.1 配置 PostgreSQL

Docker Compose 服务名：

```text
postgres
```

要求：

```text
镜像：postgres:16
端口：5432
数据库：contentflow
用户名和密码从环境变量读取
挂载 PostgreSQL 数据目录
挂载 sql/init.sql
配置健康检查
```

## 6.2 配置 Redis

服务名：

```text
redis
```

要求：

```text
镜像：redis:7
端口：6379
开启数据持久化
配置健康检查
```

## 6.3 配置 MinIO

服务名：

```text
minio
```

要求：

```text
端口 9000：API
端口 9001：控制台
启动时创建 contentflow bucket
持久化数据目录
```

可新增 `minio-init` 服务，在 MinIO 启动后自动创建 Bucket。

## 6.4 配置 Chroma

服务名：

```text
chroma
```

要求：

```text
端口：8000
开启数据持久化
挂载 chroma_data
Agent 通过 HTTP 客户端访问
```

## 6.5 第二阶段验收

执行：

```bash
docker compose up -d postgres redis minio chroma minio-init
```

确认：

```text
PostgreSQL 可以连接
Redis PING 返回 PONG
MinIO 控制台可以访问
Chroma 心跳接口正常
```

---

# 7. 第三阶段：PostgreSQL 数据库设计

## 7.1 核心表

第一版实现以下表：

```text
sys_user
cf_project
cf_document
cf_document_chunk
cf_content
cf_content_tag
cf_agent_conversation
cf_agent_message
```

## 7.2 表关系

```text
sys_user
  └── cf_project
        ├── cf_document
        │     └── cf_document_chunk
        ├── cf_content
        │     └── cf_content_tag
        └── cf_agent_conversation
              └── cf_agent_message
```

## 7.3 关键设计要求

### sys_user

字段：

```text
id
username
password
nickname
role
status
created_at
updated_at
```

约束：

```text
username 唯一
role 只允许 USER、ADMIN
status 只允许 0、1
password 保存 BCrypt 密文
```

### cf_project

字段：

```text
id
owner_id
name
platform
domain
positioning
target_audience
content_style
created_at
updated_at
```

要求：

```text
owner_id 关联 sys_user
删除用户时级联删除项目
项目名称不能为空字符串
按 owner_id + created_at 建联合索引
```

### cf_document

字段：

```text
id
project_id
owner_id
file_name
object_key
file_ext
mime_type
file_size
status
chunk_count
error_message
created_at
updated_at
```

状态：

```text
INDEXING
READY
FAILED
```

要求：

```text
删除项目时级联删除文档记录
object_key 保存 MinIO 对象路径
不要保存永久公开 URL
```

### cf_document_chunk

字段：

```text
id
document_id
project_id
chunk_index
content
chroma_id
token_count
created_at
```

要求：

```text
chroma_id 唯一
document_id + chunk_index 唯一
project_id 建索引
```

### cf_content

字段：

```text
id
project_id
owner_id
conversation_id
title
summary
markdown
content_type
status
references_json
created_at
updated_at
```

状态：

```text
DRAFT
COMPLETED
```

要求：

```text
references_json 使用 JSONB
保存 RAG 引用资料
```

### cf_content_tag

字段：

```text
id
content_id
tag_name
```

要求：

```text
content_id + tag_name 唯一
删除内容时级联删除标签
```

### cf_agent_conversation

字段：

```text
id
owner_id
project_id
thread_id
title
status
created_at
updated_at
```

要求：

```text
thread_id 使用 UUID
thread_id 唯一
```

### cf_agent_message

字段：

```text
id
conversation_id
role
content
metadata
created_at
```

role：

```text
USER
ASSISTANT
TOOL
SYSTEM
```

metadata 使用 JSONB，可保存：

```json
{
  "toolName": "search_knowledge",
  "durationMs": 300,
  "resultCount": 5
}
```

## 7.4 自动更新时间

创建数据库函数：

```sql
CREATE OR REPLACE FUNCTION cf_touch_updated_at()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = now();
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;
```

给包含 `updated_at` 的表创建更新触发器。

## 7.5 初始化数据

`sql/demo-data.sql` 中至少提供：

```text
一个管理员账号
一个普通测试账号
一个 Java成长指南项目
```

测试密码必须提前使用 BCrypt 生成，不允许使用假的 BCrypt 字符串。

## 7.6 第三阶段验收

必须确认：

```text
所有表能够正常创建
不存在重复字段
所有外键方向正确
项目删除时业务数据级联删除
updated_at 可以自动更新
JSONB 字段可以正常读写
```

---

# 8. 第四阶段：Spring Boot 后端初始化

## 8.1 创建项目

基本信息：

```text
groupId：com.contentflow
artifactId：contentflow-backend
Java：17
Spring Boot：3.x
```

## 8.2 添加依赖

至少包含：

```text
spring-boot-starter-web
spring-boot-starter-security
spring-boot-starter-validation
spring-boot-starter-data-redis
spring-boot-starter-webflux
postgresql
mybatis-plus-spring-boot3-starter
jjwt
minio
lombok
springdoc-openapi
flyway-core
flyway-database-postgresql
```

`spring-boot-starter-webflux` 主要用于 `WebClient` 和流式转发，不使用 WebFlux 重构整个业务层。

## 8.3 创建配置文件

创建：

```text
application.yml
application-dev.yml
application-prod.yml
```

配置内容：

```text
PostgreSQL
Redis
MinIO
JWT
LangGraph 服务地址
内部接口 Token
上传文件大小限制
日志级别
```

## 8.4 统一返回结构

创建：

```java
public class Result<T> {

    private Integer code;
    private String message;
    private T data;

}
```

成功示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

失败示例：

```json
{
  "code": 40001,
  "message": "项目不存在或无权访问",
  "data": null
}
```

## 8.5 统一异常处理

创建：

```text
BusinessException
GlobalExceptionHandler
ErrorCode
```

必须处理：

```text
参数校验异常
业务异常
认证异常
访问拒绝异常
文件上传异常
外部服务调用异常
未知异常
```

## 8.6 MyBatis-Plus 配置

配置：

```text
分页插件
下划线转驼峰
逻辑删除不强制使用
ID 由 PostgreSQL BIGSERIAL 生成
```

## 8.7 第四阶段验收

启动后端后确认：

```text
应用可以连接 PostgreSQL
应用可以连接 Redis
Swagger 文档可以打开
健康检查接口正常
统一异常结构生效
```

---

# 9. 第五阶段：用户认证模块

## 9.1 后端目录

```text
auth/
security/
user/
```

## 9.2 注册接口

```text
POST /api/auth/register
```

请求：

```json
{
  "username": "demo",
  "password": "123456",
  "nickname": "测试用户"
}
```

处理流程：

```text
校验用户名格式
→ 校验密码长度
→ 检查用户名是否存在
→ BCrypt 加密密码
→ 创建 USER 用户
→ 返回用户基本信息
```

## 9.3 登录接口

```text
POST /api/auth/login
```

请求：

```json
{
  "username": "demo",
  "password": "123456"
}
```

响应：

```json
{
  "token": "JWT_TOKEN",
  "tokenType": "Bearer",
  "expiresIn": 86400,
  "user": {
    "id": 1,
    "username": "demo",
    "nickname": "测试用户",
    "role": "USER"
  }
}
```

## 9.4 当前用户接口

```text
GET /api/auth/me
```

要求：

```text
读取 JWT
获取用户 ID
查询当前用户
不返回 password
```

## 9.5 Spring Security

公开接口：

```text
POST /api/auth/login
POST /api/auth/register
GET /actuator/health
Swagger 相关接口
```

认证接口：

```text
/api/**
```

内部接口：

```text
/internal/**
```

内部接口不使用用户 JWT，而是校验：

```text
X-Internal-Token
```

## 9.6 第五阶段验收

完成：

```text
用户注册
用户登录
JWT 认证
获取当前用户
错误 Token 被拒绝
被禁用用户不能登录
密码不以明文保存
```

---

# 10. 第六阶段：项目管理模块

## 10.1 接口

```text
POST   /api/projects
GET    /api/projects
GET    /api/projects/{id}
PUT    /api/projects/{id}
DELETE /api/projects/{id}
```

## 10.2 创建项目

请求：

```json
{
  "name": "Java成长指南",
  "platform": "小红书",
  "domain": "Java学习",
  "positioning": "帮助计算机专业学生学习Java项目开发",
  "targetAudience": "Java初学者和应届毕业生",
  "contentStyle": "通俗、简洁、案例化"
}
```

后端必须自动设置：

```text
owner_id = 当前登录用户 ID
```

前端不能传入 ownerId。

## 10.3 项目列表

支持参数：

```text
keyword
page
size
```

查询条件：

```text
owner_id = 当前用户
name 模糊查询
created_at 倒序
```

## 10.4 项目详情

详情需要返回：

```json
{
  "id": 1,
  "name": "Java成长指南",
  "platform": "小红书",
  "domain": "Java学习",
  "positioning": "...",
  "targetAudience": "...",
  "contentStyle": "...",
  "documentCount": 3,
  "contentCount": 5,
  "createdAt": "..."
}
```

## 10.5 删除项目

删除前必须：

```text
校验项目归属
查询项目文件列表
调用 Agent 删除 Chroma 向量
删除 MinIO 文件
删除 PostgreSQL 项目
```

第一版如果跨服务删除失败，可以记录日志并返回失败，不实现复杂分布式事务。

## 10.6 第六阶段验收

必须确认：

```text
用户只能查看自己的项目
用户不能修改其他用户项目
删除项目会删除数据库关联记录
项目名称不能为空
```

---

# 11. 第七阶段：内容管理模块

## 11.1 接口

```text
POST   /api/contents
GET    /api/contents
GET    /api/contents/{id}
PUT    /api/contents/{id}
DELETE /api/contents/{id}
```

## 11.2 创建内容

请求：

```json
{
  "projectId": 1,
  "conversationId": 2,
  "title": "Spring Boot JWT登录，看这一篇就够了",
  "summary": "介绍JWT登录的核心流程。",
  "markdown": "# 正文",
  "contentType": "ARTICLE",
  "status": "DRAFT",
  "tags": ["Java", "SpringBoot", "JWT"],
  "references": [
    {
      "documentId": 3,
      "fileName": "springboot.md",
      "chunkIndex": 2
    }
  ]
}
```

处理流程：

```text
校验项目归属
→ 写入 cf_content
→ 批量写入 cf_content_tag
→ 返回 contentId
```

## 11.3 更新内容

更新时：

```text
校验内容归属
更新主体
删除旧标签
插入新标签
```

整个过程必须使用事务。

## 11.4 内容列表

支持：

```text
projectId
status
keyword
page
size
```

## 11.5 第七阶段验收

完成：

```text
文章新增、查询、修改、删除
标签保存和更新
引用资料 JSONB 保存
用户不能访问其他用户文章
```

---

# 12. 第八阶段：MinIO 文件上传

## 12.1 MinIO 配置

创建：

```text
MinioConfig
MinioService
MinioServiceImpl
```

服务方法：

```java
String upload(
    InputStream inputStream,
    long size,
    String contentType,
    String objectKey
);

InputStream getObject(String objectKey);

void deleteObject(String objectKey);

String createPresignedUrl(String objectKey, Duration duration);
```

## 12.2 对象路径规则

使用：

```text
user-{userId}/project-{projectId}/document-{documentId}/{uuid}-{fileName}
```

不要直接使用原始文件名作为对象唯一键。

## 12.3 上传接口

```text
POST /api/projects/{projectId}/documents
Content-Type: multipart/form-data
```

处理流程：

```text
校验项目归属
→ 校验扩展名
→ 校验 MIME 类型
→ 校验文件大小
→ 创建 cf_document，status=INDEXING
→ 上传文件到 MinIO
→ 更新 object_key
→ 调用 Agent 文档解析接口
→ 返回文档信息
```

允许格式：

```text
.pdf
.txt
.md
.markdown
```

限制：

```text
单文件最大 20MB
```

## 12.4 文档接口

```text
POST   /api/projects/{projectId}/documents
GET    /api/projects/{projectId}/documents
GET    /api/documents/{id}
DELETE /api/documents/{id}
POST   /api/documents/{id}/retry
```

## 12.5 删除文档

流程：

```text
校验文档归属
→ 调用 Agent 删除文档对应 Chroma 数据
→ 删除 MinIO 对象
→ 删除 cf_document
→ 级联删除 cf_document_chunk
```

## 12.6 第八阶段验收

必须完成：

```text
PDF/TXT/MD 可以上传
文件写入 MinIO
文档记录写入 PostgreSQL
非法格式被拒绝
其他用户无法访问文档
```

---

# 13. 第九阶段：Agent 服务初始化

## 13.1 创建 Python 项目

目录：

```text
contentflow-agent/
├── pyproject.toml
├── langgraph.json
├── Dockerfile
├── .env.example
└── src/contentflow_agent/
```

## 13.2 安装依赖

至少包含：

```text
langgraph
langchain
langchain-core
langchain-openai 或实际模型 SDK
chromadb
pydantic
pydantic-settings
httpx
fastapi
uvicorn
pypdf
tiktoken
python-multipart
```

## 13.3 配置类

创建 `config.py`，读取：

```text
LLM_API_KEY
LLM_BASE_URL
LLM_MODEL
EMBEDDING_API_KEY
EMBEDDING_BASE_URL
EMBEDDING_MODEL
CHROMA_HOST
CHROMA_PORT
BACKEND_BASE_URL
INTERNAL_AGENT_TOKEN
```

## 13.4 BackendClient

创建统一客户端：

```python
class BackendClient:

    async def get_project_context(
        self,
        project_id: int,
        user_id: int
    ) -> dict:
        ...

    async def save_document_chunks(
        self,
        document_id: int,
        chunks: list[dict]
    ) -> None:
        ...

    async def update_document_status(
        self,
        document_id: int,
        status: str,
        chunk_count: int = 0,
        error_message: str | None = None
    ) -> None:
        ...

    async def save_content_draft(
        self,
        payload: dict
    ) -> dict:
        ...
```

每次请求添加：

```text
X-Internal-Token
```

## 13.5 第九阶段验收

确认：

```text
Agent 服务可以启动
可以连接 Chroma
可以调用 Spring Boot 内部接口
配置缺失时有明确错误
```

---

# 14. 第十阶段：文档解析与知识库构建

## 14.1 文档解析接口

Agent 提供：

```text
POST /documents/index
```

请求：

```json
{
  "documentId": 1,
  "projectId": 1,
  "userId": 1,
  "objectKey": "user-1/project-1/document-1/test.pdf",
  "fileName": "test.pdf",
  "fileType": "pdf"
}
```

## 14.2 文件读取方式

推荐第一版由 Spring Boot 提供内部下载接口：

```text
GET /internal/agent/documents/{documentId}/download
```

Agent 通过内部接口获得文件流。

这样 Agent 不需要直接保存 MinIO 密钥。

## 14.3 文本加载器

实现：

```text
PDF：PyPDFLoader 或 pypdf
TXT：UTF-8 文本读取
Markdown：UTF-8 文本读取
```

第一版不做 OCR。

扫描版 PDF 无文本时：

```text
标记 FAILED
error_message = PDF未包含可提取文本
```

## 14.4 文本清洗

至少处理：

```text
去除连续多余空行
去除首尾空格
统一换行符
过滤空白片段
```

## 14.5 文本分块

推荐参数：

```text
chunk_size = 800
chunk_overlap = 120
```

可根据实际模型 Token 调整。

每个切片生成：

```json
{
  "chunkIndex": 0,
  "content": "切片内容",
  "tokenCount": 320,
  "chromaId": "project_1_document_1_chunk_0"
}
```

## 14.6 写入 Chroma

Collection 名：

```text
contentflow_knowledge
```

添加数据：

```python
collection.upsert(
    ids=[...],
    documents=[...],
    metadatas=[...]
)
```

metadata 必须包含：

```json
{
  "project_id": 1,
  "document_id": 1,
  "file_name": "springboot.md",
  "chunk_index": 0,
  "owner_id": 1
}
```

## 14.7 保存 PostgreSQL 元数据

Agent 写入 Chroma 后，调用 Spring Boot：

```text
POST /internal/agent/documents/{documentId}/chunks
```

请求：

```json
{
  "projectId": 1,
  "chunks": [
    {
      "chunkIndex": 0,
      "content": "...",
      "chromaId": "project_1_document_1_chunk_0",
      "tokenCount": 320
    }
  ]
}
```

完成后调用：

```text
PUT /internal/agent/documents/{documentId}/status
```

成功：

```json
{
  "status": "READY",
  "chunkCount": 12
}
```

失败：

```json
{
  "status": "FAILED",
  "errorMessage": "PDF未包含可提取文本"
}
```

## 14.8 重试处理

重试前必须：

```text
删除 Chroma 中旧 document_id 数据
删除 PostgreSQL 旧 chunk 数据
重新执行解析
```

## 14.9 第十阶段验收

演示：

```text
上传 Spring Boot Markdown 文档
文档状态从 INDEXING 变为 READY
PostgreSQL 存在切片记录
Chroma 存在对应向量数据
不同 project_id 的数据不会混用
```

---

# 15. 第十一阶段：LangGraph 工作流

## 15.1 State 定义

```python
from typing import Annotated, Any, TypedDict

from langchain_core.messages import AnyMessage
from langgraph.graph.message import add_messages


class ContentFlowState(TypedDict, total=False):
    messages: Annotated[list[AnyMessage], add_messages]

    user_id: int
    project_id: int
    conversation_id: int

    user_request: str

    project_context: dict
    parsed_request: dict
    retrieved_chunks: list[dict]

    generated_content: dict
    error_message: str | None
```

## 15.2 主工作流

```text
START
  ↓
load_project_context
  ↓
parse_request
  ↓
need_retrieval?
  ├── yes → retrieve_knowledge
  └── no
          ↓
generate_content
  ↓
format_output
  ↓
END
```

## 15.3 load_project_context

调用：

```text
GET /internal/agent/projects/{projectId}?userId={userId}
```

返回：

```json
{
  "projectId": 1,
  "name": "Java成长指南",
  "platform": "小红书",
  "domain": "Java学习",
  "positioning": "...",
  "targetAudience": "...",
  "contentStyle": "..."
}
```

## 15.4 parse_request

使用结构化输出：

```python
class ParsedContentRequest(BaseModel):
    platform: str | None = None
    content_type: str = "ARTICLE"
    topic: str
    target_audience: str | None = None
    content_style: str | None = None
    word_limit: int | None = None
    need_retrieval: bool = True
```

输入：

```text
根据我的资料写一篇面向 Java 初学者的 JWT 登录小红书文章，控制在 800 字以内。
```

输出：

```json
{
  "platform": "小红书",
  "content_type": "ARTICLE",
  "topic": "Spring Boot JWT登录",
  "target_audience": "Java初学者",
  "content_style": "通俗、案例化",
  "word_limit": 800,
  "need_retrieval": true
}
```

## 15.5 retrieve_knowledge

调用 Chroma：

```python
collection.query(
    query_texts=[query],
    n_results=5,
    where={
        "project_id": project_id
    },
    include=[
        "documents",
        "metadatas",
        "distances"
    ]
)
```

注意：

```text
Chroma metadata 字段类型必须统一
project_id 始终存整数，查询也使用整数
```

返回结果转为：

```json
[
  {
    "documentId": 1,
    "fileName": "springboot.md",
    "chunkIndex": 3,
    "content": "...",
    "distance": 0.21
  }
]
```

## 15.6 generate_content

组合上下文：

```text
项目平台
项目定位
目标受众
内容风格
用户要求
解析后的内容要求
RAG 检索片段
```

要求模型：

```text
不要虚构不存在的引用
引用只能来自 retrieved_chunks
使用 Markdown 输出正文
遵守字数要求
标题和正文适配目标平台
```

## 15.7 format_output

结构化模型：

```python
class ReferenceItem(BaseModel):
    document_id: int
    file_name: str
    chunk_index: int


class GeneratedContent(BaseModel):
    title: str
    summary: str
    content: str
    tags: list[str]
    references: list[ReferenceItem]
```

## 15.8 编译 Graph

```python
builder = StateGraph(ContentFlowState)

builder.add_node("load_project_context", load_project_context)
builder.add_node("parse_request", parse_request)
builder.add_node("retrieve_knowledge", retrieve_knowledge)
builder.add_node("generate_content", generate_content)
builder.add_node("format_output", format_output)

builder.add_edge(START, "load_project_context")
builder.add_edge("load_project_context", "parse_request")

builder.add_conditional_edges(
    "parse_request",
    route_after_parse,
    {
        "retrieve": "retrieve_knowledge",
        "generate": "generate_content"
    }
)

builder.add_edge("retrieve_knowledge", "generate_content")
builder.add_edge("generate_content", "format_output")
builder.add_edge("format_output", END)

graph = builder.compile()
```

## 15.9 第十一阶段验收

测试以下两类请求：

需要 RAG：

```text
根据我上传的资料生成 JWT 登录文章。
```

不需要 RAG：

```text
把这段标题改得更吸引人。
```

确认工作流路由正确。

---

# 16. 第十二阶段：ReAct Agent

## 16.1 工具设计

只实现三个 Tool：

```text
get_project_context
search_knowledge
save_content_draft
```

## 16.2 get_project_context

输入：

```json
{
  "project_id": 1,
  "user_id": 1
}
```

输出：

```json
{
  "name": "Java成长指南",
  "platform": "小红书",
  "targetAudience": "Java初学者",
  "contentStyle": "通俗、案例化"
}
```

## 16.3 search_knowledge

输入：

```json
{
  "project_id": 1,
  "query": "Spring Boot JWT登录",
  "top_k": 5
}
```

必须强制：

```text
where project_id = 当前 project_id
```

不能由模型自行取消项目过滤。

## 16.4 save_content_draft

输入：

```json
{
  "project_id": 1,
  "user_id": 1,
  "conversation_id": 2,
  "title": "...",
  "summary": "...",
  "content": "...",
  "tags": [],
  "references": []
}
```

Agent 调用：

```text
POST /internal/agent/contents
```

Spring Boot 必须再次校验：

```text
user_id 是否拥有 project_id
conversation 是否属于用户和项目
```

## 16.5 自动保存规则

第一版建议：

```text
只有用户明确表达“保存”“保存为草稿”时，Agent 才调用 save_content_draft。
```

普通生成请求只返回内容，不自动保存。

## 16.6 第十二阶段验收

测试：

```text
根据项目资料生成文章
```

确认调用：

```text
get_project_context
search_knowledge
```

测试：

```text
根据资料生成文章并保存为草稿
```

确认额外调用：

```text
save_content_draft
```

---

# 17. 第十三阶段：Agent 会话模块

## 17.1 创建会话

```text
POST /api/agent/conversations
```

请求：

```json
{
  "projectId": 1,
  "title": "JWT内容创作"
}
```

后端生成：

```text
thread_id = UUID
```

## 17.2 会话列表

```text
GET /api/agent/conversations?projectId=1
```

## 17.3 会话消息

```text
GET /api/agent/conversations/{id}/messages
```

按：

```text
created_at ASC
```

返回。

## 17.4 发送消息

```text
POST /api/agent/conversations/{id}/messages
```

请求：

```json
{
  "content": "根据资料生成一篇 JWT 登录文章"
}
```

处理：

```text
校验会话归属
→ 保存 USER 消息
→ 构造 Agent 请求
→ 调用 LangGraph
→ 获取结果
→ 保存 ASSISTANT 消息
→ 返回结果
```

## 17.5 第十三阶段验收

确认：

```text
可以创建会话
可以发送多轮消息
历史消息可以查询
会话不能跨用户访问
```

---

# 18. 第十四阶段：SSE 流式输出

## 18.1 推荐接口设计

采用一个 POST 流式接口：

```text
POST /api/agent/conversations/{id}/stream
Content-Type: application/json
Accept: text/event-stream
```

请求：

```json
{
  "content": "生成一篇 JWT 登录文章"
}
```

响应事件：

```text
event: start
data: {"messageId":100}

event: node
data: {"node":"parse_request","message":"正在解析创作要求"}

event: node
data: {"node":"retrieve_knowledge","message":"已检索到5个知识片段"}

event: token
data: {"content":"Spring Boot"}

event: token
data: {"content":" JWT 登录"}

event: reference
data: {"documentId":1,"fileName":"springboot.md","chunkIndex":3}

event: complete
data: {"title":"...","summary":"...","content":"...","tags":[],"references":[]}

event: error
data: {"message":"生成失败"}
```

## 18.2 后端实现

推荐使用：

```text
Flux<ServerSentEvent<String>>
```

Spring Boot 使用 `WebClient` 调用 Agent 流式接口，并将事件转发给 Vue。

## 18.3 断开处理

前端断开连接时：

```text
停止向浏览器发送数据
记录日志
不要重复保存 ASSISTANT 消息
```

只在收到 Agent `complete` 事件后保存完整 ASSISTANT 消息。

## 18.4 第十四阶段验收

确认：

```text
前端能够逐步显示生成内容
生成完成后内容结构完整
断流不会生成重复消息
错误可以显示在前端
```

---

# 19. 第十五阶段：Vue 前端初始化

## 19.1 创建项目

```bash
npm create vite@latest contentflow-web -- --template vue-ts
```

安装：

```text
vue-router
pinia
axios
element-plus
markdown-it
dompurify
```

可选编辑器：

```text
md-editor-v3
```

## 19.2 路由

```text
/login
/register
/projects
/projects/:id
/projects/:id/documents
/projects/:id/creation
/projects/:id/contents
/contents/:id
```

## 19.3 Axios 封装

实现：

```text
baseURL
请求超时
自动添加 Authorization
统一处理业务错误
401 自动清除 Token 并跳转登录
```

## 19.4 Pinia Store

至少创建：

```text
user.ts
project.ts
agent.ts
```

## 19.5 路由守卫

逻辑：

```text
没有 Token 访问业务页面 → 跳转 /login
有 Token 访问 /login → 跳转 /projects
首次进入系统 → 调用 /api/auth/me
```

## 19.6 第十五阶段验收

完成：

```text
Vue 项目启动
Element Plus 生效
登录路由守卫生效
Axios 可以调用后端
```

---

# 20. 第十六阶段：前端页面开发

## 20.1 登录页

功能：

```text
用户名输入
密码输入
登录按钮
跳转注册
错误提示
```

## 20.2 注册页

功能：

```text
用户名
昵称
密码
确认密码
注册
```

## 20.3 项目列表页

功能：

```text
项目卡片
新建项目
编辑项目
删除项目
进入项目详情
分页
关键词搜索
```

## 20.4 项目详情页

展示：

```text
项目名称
平台
领域
账号定位
目标受众
内容风格
资料数量
内容数量
```

操作：

```text
管理资料
进入 AI 创作
查看内容
编辑项目
```

## 20.5 素材管理页

功能：

```text
拖拽上传
上传进度
资料列表
状态标签
失败原因
重新解析
删除
```

状态颜色：

```text
INDEXING：黄色
READY：绿色
FAILED：红色
```

## 20.6 AI 创作页

页面结构：

```text
┌──────────────┬──────────────────────┬──────────────────────┐
│ 项目资料列表  │ AI 对话区域           │ 内容预览区域          │
│              │                      │                      │
│ READY资料     │ 历史消息              │ 标题                  │
│ 文件名        │ 用户输入              │ 摘要                  │
│ 状态          │ Agent流式输出         │ Markdown正文          │
│              │ 输入框和发送按钮      │ 标签和引用            │
│              │                      │ 保存按钮              │
└──────────────┴──────────────────────┴──────────────────────┘
```

需要组件：

```text
DocumentList
ChatPanel
ChatMessage
CreationPreview
ReferenceList
```

## 20.7 内容列表页

展示：

```text
标题
摘要
状态
创建时间
更新时间
```

筛选：

```text
全部
草稿
已完成
```

## 20.8 内容详情页

功能：

```text
查看标题
查看摘要
Markdown 编辑
标签编辑
查看引用资料
保存
标记完成
删除
```

## 20.9 第十六阶段验收

必须完成六个核心页面并能调用真实后端接口。

---

# 21. 第十七阶段：前后端与 Agent 联调

## 21.1 登录联调

验证：

```text
注册
登录
Token 自动携带
刷新后恢复登录状态
```

## 21.2 项目联调

验证：

```text
创建项目
编辑项目
查看列表
删除项目
```

## 21.3 文档联调

验证：

```text
上传文件
查看 INDEXING
Agent 完成解析
状态更新为 READY
删除文件
```

## 21.4 Agent 联调

验证：

```text
创建会话
发送创作要求
LangGraph 获取项目上下文
Chroma 检索知识
SSE 流式返回
前端显示引用
```

## 21.5 内容联调

验证：

```text
保存生成文章
内容列表可见
详情可编辑
状态可修改为 COMPLETED
```

---

# 22. 第十八阶段：Docker 化

## 22.1 后端 Dockerfile

使用多阶段构建：

```text
Maven 构建
JRE 运行
```

## 22.2 前端 Dockerfile

使用：

```text
Node 构建
Nginx 运行
```

## 22.3 Agent Dockerfile

要求：

```text
Python 3.12
安装 pyproject 依赖
启动 LangGraph 或 FastAPI
```

## 22.4 Docker Compose 完整服务

```text
postgres
redis
minio
minio-init
chroma
backend
agent
frontend
nginx
```

依赖顺序：

```text
backend 等待 postgres、redis、minio
agent 等待 chroma、backend
frontend 只依赖构建
nginx 等待 backend、frontend
```

## 22.5 Nginx

反向代理：

```text
/          → frontend
/api/      → backend:8080
```

SSE 配置：

```nginx
proxy_buffering off;
proxy_cache off;
proxy_read_timeout 3600s;
```

## 22.6 第十八阶段验收

执行：

```bash
docker compose up -d --build
```

确认浏览器可以完成全部核心流程。

---

# 23. 第十九阶段：测试

## 23.1 后端单元测试

至少覆盖：

```text
AuthService
ProjectService
ContentService
项目归属校验
文档状态更新
```

## 23.2 后端接口测试

至少覆盖：

```text
登录成功和失败
未登录访问被拒绝
跨用户项目访问被拒绝
文件格式校验
内容保存事务
```

## 23.3 Agent 测试

至少覆盖：

```text
parse_request 结构化输出
项目级 Chroma 过滤
无检索结果时正常生成
Pydantic 输出校验
文档解析失败状态回写
```

## 23.4 前端测试

第一版可只进行核心手工测试：

```text
登录
项目 CRUD
上传文件
AI 生成
保存文章
```

---

# 24. 第二十阶段：安全检查

必须检查：

```text
JWT Secret 不写死
数据库密码不提交 Git
MinIO 密钥不提交 Git
内部接口 Token 不写死
用户不能传 ownerId 控制数据归属
所有资源访问均校验 owner_id
上传文件名经过处理
限制文件大小和扩展名
Markdown 预览防止 XSS
内部接口不暴露到公网
日志中不打印 Token 和密码
```

---

# 25. 第二十一阶段：日志与错误处理

## 25.1 后端日志

记录：

```text
登录失败
项目访问越权
文档上传
Agent 请求开始和结束
Agent 调用失败
文档解析失败
SSE 连接断开
```

## 25.2 Agent 日志

记录：

```text
thread_id
project_id
conversation_id
节点开始和结束
检索片段数量
工具调用名称
模型调用耗时
异常信息
```

禁止记录：

```text
JWT
数据库密码
完整 API Key
用户密码
```

---

# 26. 第二十二阶段：README

README 必须包含：

```text
项目介绍
核心功能
系统架构
技术栈
目录结构
环境要求
本地启动方法
Docker Compose 启动方法
默认测试账号
接口文档地址
MinIO 控制台地址
Chroma 地址
演示流程
常见问题
```

本地启动顺序：

```text
1. 启动 PostgreSQL、Redis、MinIO、Chroma
2. 启动 Spring Boot
3. 启动 LangGraph Agent
4. 启动 Vue
```

---

# 27. 推荐开发顺序

Codex 必须按照以下顺序执行，不要同时生成全部模块。

## 阶段一：基础工程

```text
根目录
Docker Compose 基础设施
数据库脚本
Spring Boot 初始化
Vue 初始化
Agent 初始化
```

## 阶段二：基础业务

```text
用户认证
项目 CRUD
内容 CRUD
前端基础页面
```

## 阶段三：文件知识库

```text
MinIO 上传
文档管理
Agent 文档解析
文本分块
Embedding
Chroma 写入
项目级检索
```

## 阶段四：AI 生成

```text
LangGraph State
需求解析
RAG 检索
内容生成
Pydantic 结构化输出
```

## 阶段五：ReAct

```text
get_project_context
search_knowledge
save_content_draft
ContentAgent
```

## 阶段六：流式联调

```text
会话管理
Spring Boot 调用 Agent
SSE 转发
Vue 流式展示
内容保存
```

## 阶段七：部署验收

```text
Dockerfile
Docker Compose 完整部署
Nginx
测试
README
```

---

# 28. 每阶段 Codex 输出要求

每完成一个阶段，Codex 必须输出：

```text
1. 本阶段完成内容
2. 新增文件列表
3. 修改文件列表
4. 数据库变更
5. 接口列表
6. 启动命令
7. 测试步骤
8. 已知问题
9. 下一阶段任务
```

Codex 不允许只输出说明，必须实际创建代码文件。

遇到无法确定的第三方模型配置时：

```text
使用环境变量
提供 Mock 实现
保证项目在未配置真实模型时也能启动
```

---

# 29. Mock 模式

为了保证首次启动可演示，建议增加：

```env
AI_MOCK_ENABLED=true
```

Mock 模式下：

```text
文档仍然正常解析和写入 Chroma
内容生成返回固定结构化文章
SSE 仍按事件逐步输出
不调用真实大模型
```

Mock 示例内容：

```json
{
  "title": "Spring Boot JWT 登录，看这一篇就够了",
  "summary": "介绍 JWT 登录的基本流程和关键实现。",
  "content": "# Spring Boot JWT 登录\n\n这是 Mock 模式生成的文章。",
  "tags": ["Java", "SpringBoot", "JWT"],
  "references": []
}
```

配置真实模型后关闭 Mock：

```env
AI_MOCK_ENABLED=false
```

---

# 30. 最终验收流程

项目最终必须完整演示：

1. 打开系统注册普通用户。
2. 使用新用户登录。
3. 创建“Java成长指南”项目。
4. 填写内容平台为“小红书”。
5. 填写目标受众为“Java 初学者和应届毕业生”。
6. 填写内容风格为“通俗、简洁、案例化”。
7. 上传一份 Spring Boot Markdown 或 PDF 资料。
8. 文档状态显示为 `INDEXING`。
9. Agent 完成解析后状态变为 `READY`。
10. 进入 AI 创作页面。
11. 创建一个新的 Agent 会话。
12. 输入“根据资料生成一篇 Spring Boot JWT 登录文章，控制在 800 字以内”。
13. LangGraph 加载项目上下文。
14. LangGraph 解析用户创作要求。
15. Agent 使用 `project_id` 从 Chroma 检索资料。
16. 页面通过 SSE 显示生成过程。
17. 页面展示标题、摘要、正文、标签和引用资料。
18. 用户点击保存文章。
19. 内容列表中可以查看该文章。
20. 用户进入内容详情页进行修改。
21. 用户将文章状态修改为 `COMPLETED`。
22. 用户退出登录。

---

# 31. 第一版明确不开发

第一版禁止扩展以下功能：

```text
多 Agent 协作
Agent 审批循环
自动反思循环
长期记忆
复杂 Prompt 管理后台
多租户
复杂 RBAC
微服务
消息队列
Elasticsearch
pgvector
知识图谱
OCR
图片生成
视频生成
第三方平台发布
发布日历
热点搜索
数据分析
内容审核工作流
复杂运营后台
```

---

# 32. 最终完成标准

项目达到以下标准才算完成：

```text
Spring Boot 可以独立启动
Vue 可以独立启动
Agent 服务可以独立启动
Docker Compose 可以统一启动
PostgreSQL 保存业务数据
MinIO 保存原始文件
Chroma 保存文档向量
LangGraph 工作流真实运行
ReAct Agent 可以调用三个工具
RAG 检索严格按照 project_id 隔离
SSE 可以流式输出
文章可以保存、编辑和删除
用户数据相互隔离
README 启动步骤完整
首次启动可以使用 Mock 模式演示
```

最终系统结构：

```text
Vue 3
  ↓
Spring Boot
  ├── PostgreSQL
  ├── Redis
  ├── MinIO
  └── LangGraph Agent
          ├── LLM
          └── Chroma
```

第一版的核心成果应控制为：

```text
一个 Spring Boot 单体后端
一个 Vue 3 前端
一个 LangGraph Agent 服务
一个 LangGraph 主工作流
一个 ReAct ContentAgent
三个 Agent Tool
一套项目级 RAG
八张 PostgreSQL 核心表
六个核心前端页面
一套 Docker Compose 部署
```

这版适合直接交给 Codex 分阶段生成，建议每次只让它执行一个阶段，完成并跑通后再继续下一阶段。
